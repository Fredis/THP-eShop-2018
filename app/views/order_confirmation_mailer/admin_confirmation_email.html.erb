<body>
	
	<p> Yo, <p>
	<p> Bonne nouvelle, l'utilisateur <%=@user.id%> a claqué ses bouzoufs pour acheter des photos de chats t'as vu </p>
	<p> Voici le récapitulatif de sa commande <p>
	<p> Commande numéro <%=@order.id%></p>
	 <% if @order.items.size == 1 %>
		<%= @order.items.first.title %> <br />
		<p> Url de l'image <%=@order.items.first.image_url%> </p>
		<p> Total de la commande <%=@order.items.first.price %> </p> 
		<p> Miaou ! 😽 </p>


	<% else %>
		<% @order.items.each do |o| %>
			<%= o.title %> <br />
			<%= o.image_url %> <br />
			<%= o.price %> <br />
		<% end %>
		<p>Total de la commande <%=@order.items.sum(:price)%></p>
		<p> Miaou ! 😽 </p>
	<%end%>



</body>

