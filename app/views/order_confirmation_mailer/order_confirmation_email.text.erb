	Bonjour,
	Merci pour votre commande sur Pic'your Cat. En voici le récapitulatif
	Numéro de commande <%=@order.id%>
	<% if @order.items.size == 1 %> 
		<%= @order.items[0].title %>
		Voici votre image 
		Vous pouvez également récupérer votre image via le lien <%= @order.items.first.image_url %>
		Voici le total de la commande <%=@order.items.first.price %>
		A très bientôt
		Miaou ! 😽

	<% else %>
		<% @order.items.each do |o| %>
			<%= o.title %>
			Lien vers l'image <%= o.image_url %>
		<% end %>
		Total de la commande <%=@order.items.sum(:price)%>
		A très bientôt
		Miaou ! 😽

	<%end%>

