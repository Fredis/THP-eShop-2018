	 Yo,
	Bonne nouvelle, l'utilisateur <%=@user.id%> a claqué ses bouzoufs pour acheter des photos de chats t'as vu !
	Voici le récapitulatif de sa commande
	Commande numéro <%=@order.id%>
	 <% if @order.items.size == 1 %>
		<%= @order.items[0].title %>
		Url de l'image <%=@order.items[0].url_image%>
		Total de la commande <%=@order_item[0].price %> 
		Miaou ! 😽 


	<% else %>
		<% @order.items.each do |o| %>
			<%= o.title %>
			<%= o.image_url %>
			<%= o.price %>
		<% end %>
		Total de la commande <%=@order.items.sum(:price)%>
		Miaou ! 😽 
	<%end%>


