	 Yo,
	Bonne nouvelle, l'utilisateur <%=current_user.id %> a claqué ses bouzoufs pour acheter des photos de chats t'as vu !
	Voici le récapitulatif de sa commande
	Commande numéro <%=order.id%>
	 <% if items_to_mail.size == 1 %>
		<%= items_to_email[0].title %>
		Url de l'image <%=items_to_email[0].url_image%>
		Total de la commande <%=items_to_email[0].price %> 
		Miaou ! 😽 


	<% else %>
		<% items_to_email.each do |i| %>
			<%= i[i.id].title %>
			<%= i[i.id].image_url %>
			<%= i[i.id].price %>
		<% end %>
		Total de la commande <%=items_to_email.sum(:price)%>
		Miaou ! 😽 
	<%end%>

