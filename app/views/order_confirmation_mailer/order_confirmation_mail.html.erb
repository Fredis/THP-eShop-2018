<body>
	
	<p> Bonjour, <p>
	<p>Merci pour votre commande sur Pic'your Cat. En voici le récapitulatif</p>
	<p>Numéro de commande <%=@order.id%></p>
	<% if @order.items.count == 1 %> 
		<%= @order.items.first.title %> <br />
		<p>Voici votre image </p>
		<%= image_tag 'items/#{@order.items.first.image_url}'%>>
		<p> Voici le total de la commande <%=order.items[0].item.price %> </p>
		<p> A très bientôt </p>
		<p> Miaou ! 😽 </p>

	<% else %>
		<% @order.items.each do |o| %>
			<%= o.title %> <br />
			<%= image_tag 'items/{o.image_url}'%>
		<% end %>
		<p>Total de la commande <%=@order.items.sum(:price)%></p>
		<p> A très bientôt </p>
		<p> Miaou ! 😽 </p>

	<%end%>
</body>

